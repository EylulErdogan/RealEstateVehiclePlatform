@model RealEstateVehiclePlatform.WebUI.ViewModels.ListingType.CreateListingTypeViewModel

@{
    ViewData["Title"] = "Yeni İlan Tipi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="admin-card">
    <div class="mb-4">
        <h4 class="fw-bold">Yeni İlan Tipi</h4>
        <small class="text-muted">Satılık veya kiralık ilan tipi ekleyebilirsiniz.</small>
    </div>

    <form asp-action="Create" method="post">
        <div class="mb-4">
            <label class="form-label fw-semibold">İlan Tipi Adı</label>
            <input asp-for="Name" class="form-control form-control-lg" />
        </div>

        <div class="d-flex gap-2">
            <button class="btn btn-primary">
                <i class="bi bi-check-circle"></i>
                Kaydet
            </button>

            <a href="/AdminListingType" class="btn btn-light">
                Vazgeç
            </a>
        </div>
    </form>
</div>